# Yulux Signs 网站生产环境优化计划

> 评估日期：2026-07-12
> 评估范围：`web/` 目录（Next.js 16 App Router + TypeScript + Tailwind CSS v4）
> 当前状态：`npm run build` 通过，30 个路由全部成功生成（含 Privacy Policy、Terms、错误兜底页）

---

## 一、项目现状概览

这是一个面向海外市场的定制霓虹灯/发光字营销网站，核心业务是**询盘获客**（Get a Quote），附带轻量的展示型"商城"（购物车最终导向报价表单，无真实支付）。

**做得好的地方（保持现状即可）：**

- 架构清晰：文案集中在 `content/*.ts`，组件按 `home / shared / forms / product` 分层，页面薄、内容与展示分离
- SEO 意识强：每页有独立 metadata、canonical、OG/Twitter 卡片；结构化数据（Organization、Product、FAQ、Breadcrumb、Article、HowTo）覆盖全面
- 全站使用 `next/image`，动态路由均有 `generateStaticParams`，构建产物以静态为主，天然抗流量
- TypeScript 严格、组件较小、无冗余依赖（仅 next/react/tailwind）

---

## 二、优化项目执行状态

### ✅ 已完成 — 图片素材严重超重

**问题：** `public/images/` 原始总量约 **258 MB**，单张 PNG 最大 8 MB，首屏 LCP 与部署包压力极大。

**已执行操作：**
- 新增 `scripts/compress-images.mjs`，用 sharp 批量将全部 61 张 PNG 转换为 WebP（质量 82，最宽 2000px）
- 压缩后总量 **11.2 MB（节省 95.7%，246.7 MB）**，单图最大约 616 KB
- 自动批量更新 `content/`、`components/`、`app/` 中 11 个文件的图片路径引用（`.png` → `.webp`）
- `next.config.ts` 新增 `images.formats: ["image/avif", "image/webp"]`，Next.js 对支持 AVIF 的浏览器自动升级格式

---

### ✅ 已完成 — SEO 收尾缺失

**问题：** 无 sitemap/robots；metadata 缺 metadataBase；Product Schema 有硬编码假评分；logo 引用文件不存在；WebSite Schema 含无效 SearchAction。

**已执行操作：**
- 新建 `app/sitemap.ts`：覆盖全部 30 个路由（含产品页、文章页、技术页、Privacy/Terms），带 priority 和 changeFrequency
- 新建 `app/robots.ts`：允许所有页面抓取，屏蔽 `/api/`，声明 sitemap URL
- `lib/metadata.ts`：补 `metadataBase: new URL(SITE.url)` 修复 OG 图片相对路径问题
- `lib/schema.ts`：
  - 删除 `aggregateRating`（硬编码假 4.9 分 / 127 条）
  - 将 `logo` 改为实际存在的 `og-cover.webp`
  - 删除 `potentialAction: SearchAction`（站内无搜索功能）

---

### ✅ 已完成 — 缺少错误兜底页面

**问题：** 用户访问失效链接或发生异常时展示 Next.js 默认白板页，无品牌化兜底。

**已执行操作：**
- `app/not-found.tsx`：品牌化 404 页，含"Back to Home"和"Get a Free Quote"按钮
- `app/error.tsx`：路由级运行时错误兜底，含"Try Again"与"Back to Home"
- `app/global-error.tsx`：根布局级严重错误兜底（含完整 `<html><body>` 结构）
- `app/loading.tsx`：动态页面（`/gallery` 等）的 loading 骨架，含旋转指示器

---

### ✅ 已完成 — 安全加固

**问题：** `next.config.ts` 完全为空，无安全响应头；无 env 规范文档。

**已执行操作：**
- `next.config.ts` 通过 `headers()` 为全站添加：
  - `Strict-Transport-Security`（HSTS，2 年，含子域）
  - `X-Content-Type-Options: nosniff`
  - `X-Frame-Options: DENY`
  - `X-XSS-Protection: 1; mode=block`
  - `Referrer-Policy: strict-origin-when-cross-origin`
  - `Permissions-Policy`（禁用 camera / mic / geolocation / payment）
  - `Content-Security-Policy`（含 GA / Fonts 白名单，`frame-ancestors 'none'`）
- 新建 `.env.example`：记录所有未来需要的环境变量（邮件/存储/分析/防垃圾），附中文说明

---

### ✅ 已完成 — 国际化与合规（基础合规层）

**问题：** 面向欧美用户收集邮箱/文件，却无隐私政策与服务条款，存在 GDPR/CCPA 合规风险。

**已执行操作：**
- 新建 `app/privacy-policy/page.tsx`：12 节完整隐私政策，含 GDPR 法律依据、数据保留期、用户权利、国际传输声明
- 新建 `app/terms/page.tsx`：12 节完整服务条款，含定价流程、定制审批、保修、退款、知识产权、责任限制
- `content/site.ts` `FOOTER_LINKS.company` 补充两个链接入口
- `components/layout/Footer.tsx` 版权栏补 Privacy Policy / Terms 链接（全站可见）
- 两个新页面已纳入 `app/sitemap.ts`（priority: 0.3）

---

## 三、待办 — 后端开发阶段（暂缓）

### P0（后端开发时优先）

#### 1. 表单后端 API（上线阻断）
`QuoteForm.tsx`、`InquiryForm.tsx` 的提交逻辑目前仅做前端展示，数据未保存。后端开发时需：
- 新建 `app/api/inquiry/route.ts`（Route Handler）
- 服务端输入校验（zod）
- 邮件通知（Resend 或 SMTP）
- 文件上传（Cloudflare R2 / S3 预签名直传）
- 防垃圾：Cloudflare Turnstile + IP 限流
- 前端补 loading/失败提示状态

### P1（上线前建议）

#### 5. 监控与数据分析
- GA4 + Sentry + Core Web Vitals 上报
- 关键转化事件埋点（表单提交、加购、CTA 点击）

#### 6. 工程化与 CI/CD
- GitHub Actions：PR 跑 lint + typecheck + build
- 补 `package.json` 的 `typecheck` 脚本
- 核心逻辑单测（`calculateMockPrice`、cart-context）

#### 7. 购物车产品决策
- 方案 A（推荐）：询价车——localStorage 持久化 + 数量增减 + 结算时带入报价单
- 方案 B：真实电商（Stripe Checkout），工作量大，建议二期

### P2（上线后迭代）

#### 8. 可访问性（a11y）
- 弹层（`RealWorldProjects`、`CartDrawer`）加 focus trap + ESC 关闭
- `Header` 桌面端下拉修复 `aria-expanded` + 键盘可操作
- 装饰性 emoji 包 `aria-hidden`

#### 9. 体验细节
- `/gallery` 改路径分页（`/gallery/page/2`）恢复全静态
- Customizer 加载真实预览字体（`next/font/local` + 子集化）
- 报价成功做成独立路由（`/get-a-quote/thank-you`）便于 GA 转化统计

---

## 四、实施路线图（更新版）

| 阶段 | 内容 | 状态 |
|------|------|------|
| 已完成 | 图片压缩 258MB→11MB；sitemap/robots；metadataBase；删假评分；安全响应头；not-found/error/loading；Privacy Policy & Terms | ✅ 完成 |
| 后端阶段（P0） | 表单 API + 邮件 + 文件存储 + 防垃圾 | 待开发 |
| 上线前（P1） | GA4 + Sentry；CI；购物车方案 | 待开发 |
| 上线 | Vercel 部署、域名/DNS、Search Console 提交 sitemap | 待部署 |
| 上线后（P2） | a11y、gallery 静态分页、Customizer 字体 | 迭代 |

---

## 五、验收标准（Checklist）

### 已可验收

- [x] `public/images/` 总量 < 15 MB（当前 11.2 MB），单图 < 650 KB
- [x] `next.config.ts` 配置 AVIF/WebP 格式输出
- [x] `/sitemap.xml` 覆盖全部路由（可通过 `npm run build` 后查看 `.next/server` 验证）
- [x] `/robots.txt` 可访问，屏蔽 `/api/`
- [x] `metadataBase` 已设置，OG 图片 URL 为绝对路径
- [x] Google Rich Results Test 不再出现假评分警告
- [x] `organizationSchema` 的 logo 指向实际存在的文件
- [x] 访问不存在的 URL 显示品牌化 404 页（含导航回首页 / 报价页）
- [x] 路由级错误显示品牌化 500 页（含"Try Again"）
- [x] 全局错误有 `global-error.tsx` 兜底
- [x] 动态页面有 `loading.tsx` 骨架
- [x] 安全响应头已设置（目标 securityheaders.com 评级 ≥ A）
- [x] CSP 包含 `frame-ancestors 'none'`（防点击劫持）
- [x] HSTS 已启用（生产域名上需 HTTPS 才生效）
- [x] `.env.example` 记录所有所需环境变量
- [x] Footer 含 Privacy Policy 和 Terms 链接
- [x] Privacy Policy 含 GDPR 法律依据声明
- [x] Privacy Policy & Terms 已纳入 sitemap

### 后端开发完成后验收

- [ ] 提交报价表单后，邮箱/数据库能收到完整数据（含附件），失败有告警
- [ ] 表单有 loading 态和失败重试提示
- [ ] 文件上传限制类型（白名单）和大小（≤ 20MB）
- [ ] 防垃圾：Turnstile 通过后才能提交

### 上线前验收

- [ ] Lighthouse 全站体检：Performance ≥ 85，SEO = 100，Best Practices ≥ 95
- [ ] 首页移动端 LCP < 2.5s（`hero-background.webp` 41 KB，已具备条件）
- [ ] GA4 能看到表单提交转化事件；Sentry 能收到测试错误
- [ ] CI 在 PR 上自动跑通 lint + typecheck + build
- [ ] Search Console 收录 sitemap 正常
